import {Body} from "game/body/Body";
import {GameState} from "game/GameState";

// The name should be the name of the "tile" or "block" followed by Body.
// EX: SandBody or GlassBody
export class ____Body extends Body {
    name: string;
    width: number = 12;
    height: number = 12;

    constructor(x: number, y: number, game: GameState) {
        super(x, y, game);
    }


    // After adding your png file to the /assets/sprites directory, run 'npm run transformSprites' in the terminal.
    // Once run, the sprite will be turned into text that you can copy and paste.
    // The text file will be in /dist/sprites, and share the name with the image file but with .txt at the end instead.
    // Copy and paste the contents of the text file in the backticks.
    sprite(): string {
        return `____`;
    }

}